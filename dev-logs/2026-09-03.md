# 开发日志 2026-09-03

## 今日完成
- 排查数据丢失问题：确认不是代码 bug（所有删除操作均需用户手动触发），最可能原因是华为手机系统/浏览器清理了 IndexedDB 网站数据（用户手机内存紧张）
- 新增云备份功能（v1.4.10 → v1.4.11，未提交）：
  - `src/utils/cloudBackup.ts`：备份全部 16 张表到 GitHub 私有仓库（gzip 压缩 + 只保留最近 10 份），支持从云端恢复（覆盖式）
  - `DataManagePage`：新增「云端备份」卡片（状态显示、立即备份、从云端恢复、设置：用户名/仓库名/令牌）+ 自动备份间隔下拉（5分钟~12小时）
  - `App.tsx`：打开应用时自动检查，超过设置的间隔未备份则静默上传
- 修复来信名字时对时错：`incomingLetter.ts` / `letterStore.ts` 改为直接读数据库昵称（原来是读可能未加载完的设置 store）；打开书信页时自动修复旧的"致我…他"错误格式信件
- 每日记录：过去日期不再自动生成"他的标签"（只有当天生成）
- 导入支持空值覆盖：文字导入和 JSON 导入中，没有"他的标签"的行会清空目标日期标签；JSON 导入空占位记录可被覆盖
- 数据管理页新增「清理历史脏标签」按钮：先统计数量 → 用户确认 → 清空无内容日期的标签
- 聊天设置新增来信频率滑杆：最短/最长来信间隔（小时，默认 24~42，范围 1~168），已加入导出备份
- 用户测试反馈的 3 个问题修复（v1.5.2，未提交）：
  1. 自动云备份只在打开应用时检查一次 → 改为每分钟定时检查（内部按间隔判断）；备份成功后发 `zika:cloudBackupDone` 事件刷新页面显示的"上次备份"时间
  2. 每日记录导入后日历不显示对方标签 → 原因是导入记录没有 partnerMoodTime，日历条件改为"没有揭晓时间视为已揭晓"；文字导入同时把 partnerMoodTime 设为 0
  3. 聊天状态栏消失 → 排查为数据丢失后"他的/通用"情绪标签缺失（非代码问题），指导用户从旧备份导入或重建

## 待办事项
- 等用户测试云备份/恢复和以上修复
- 用户确认后提交推送 v1.5.2

## 遇到的问题
- TypeScript 6 对 `Uint8Array` 泛型更严格，Blob 构造需 `Uint8Array<ArrayBuffer>` 类型
- 用户数据丢失：IndexedDB 被系统清理后无法找回，只能靠备份文件恢复
- 来信名字 bug 根源是设置 store 未加载完就生成信件（竞态），改为直接读数据库解决
