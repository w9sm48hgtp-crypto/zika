# 开发日志 2026-07-13

## 今日完成

### 记录模块重构 (v1.7.5 → v1.8.4)
- 底部导航 "书信" → "记录"，路由 `/records`
- 旧路由 `/letters` 自动跳转到 `/records/letters`
- 新增记录首页（RecordsPage）：书信 + 日常两个入口卡片
- 新增日常子页面（DailyLifePage）：文字便签 + 相册两个入口
- 新增文字便签页面（TextNotesPage）：独立便签，便签纸+胶带风格
- 新增相册页面（AlbumPage）：分类管理、照片导入（base64存储）、缩略图网格
- 新增照片详情页（PhotoDetailPage）：大图展示、编辑说明、前后翻页
- 数据库 v7：新增 photoAlbums、photos、stickyNotes 三张表
- 书信页添加返回按钮

### 书信 UI 优化
- 修复换行自动空两格：所有称呼后文本行一律视为新段落
- letterLine 缩进逻辑简化：i >= 1 即为新段落

### 每日页面小优化
- 选中日期蓝框从 outline 改为 box-shadow，圆角跟随 border-radius
- 小纸条输入框添加 min-width: 0 防止添加按钮溢出

### 记录卡片样式调整
- 移除入口卡片的 emoji 图标
- 标题和描述同行显示，间距 16px
- 卡片左侧稍加内边距，文字不贴边

### 第二轮优化 (v1.8.4 → v1.9.1)
- 数据管理新增「文字便签」和「相册数据」导出导入模块
- 修复手机端长按引用被浏览器文字选中/复制菜单覆盖（CSS user-select + onContextMenu）
- 相册分类折叠时自动显示前3张照片预览，超过3张显示三个小圆点，无照片显示提示
- 全部操作按钮统一改为灰色圆圈"…"按钮 + 弹出菜单（相册/便签/小纸条/Todo/书信）
- 用户信件每行自动空两格缩进（text-indent: 2em），回信不缩进
- Todo新建分类输入框背景改白色、取消按钮加白底边框
- 去掉Todo分类名称前的文件夹emoji
- 修复书信卡片和相册卡片 overflow:hidden 裁切弹出菜单

## 待办事项
- 无

## 遇到的问题
- button 元素默认 text-align: center 导致文字不靠左，添加 text-align: left 解决
- 编辑 RecordsPage.module.css 时 old_string 匹配范围不当导致 CSS 断裂，用 Write 重写修复
- tsc -b 增量编译缓存导致误报错误，--clean 清理后正常
