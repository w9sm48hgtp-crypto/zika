# 开发日志 2026-07-21

## 今日完成

### Bug 修复 (v1.4.5)
- **每日记录导入为空占位记录拦截**：每天打开每日页面会自动生成空占位记录，导致导入时"全部重复"无法导入。改为空占位覆盖更新、有内容才跳过
- **表情包连续添加**：WebView 不支持文件多选时，新增"继续添加"按钮，可逐张连续添加表情包
- 合并之前会话遗留的改动：聊天状态栏持久化、书信多行 ↵ 处理、便签加时间戳、全角分隔符兼容、JSON 清理 BOM

### 表情包多选改进 (v1.4.6)
- **File System Access API 优先**：使用 `showOpenFilePicker()` 替代传统 `<input multiple>`，可能绕过 WebView 的多选限制
- **三级回退策略**：File System Access API → `<input multiple>` → 连续添加模式

## 待办事项
- 无

## 遇到的问题
- 表情包多选失效是"网站转APP" WebView 限制，先尝试 File System Access API 解决，不行再回退
